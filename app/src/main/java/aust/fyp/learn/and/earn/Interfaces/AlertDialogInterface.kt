package aust.fyp.learn.and.earn.Interfaces

import android.content.DialogInterface

interface AlertDialogInterface {

    fun positiveButtonClick(dialogInterface: DialogInterface)
    fun negativeButtonClick(dialogInterface: DialogInterface)
}